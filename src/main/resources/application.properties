spring.application.name=TrackMoney

# PostgreSQL DB configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/trackmoneyDB
spring.datasource.username=postgres
spring.datasource.password=PostgreSQL@090

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect



USER_FOUND=User has registered already.
USER_NOT_FOUND=User is not registered.
INVALID_CREDENTIALS=Invalid Credentials.
